import { FormatNumberKDecimalPipe } from './format-number-kdecimal.pipe';

describe('FormatNumberKDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberKDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
