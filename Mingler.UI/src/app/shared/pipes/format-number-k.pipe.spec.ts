import { FormatNumberKPipe } from './format-number-k.pipe';

describe('FormatNumberKPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberKPipe();
    expect(pipe).toBeTruthy();
  });
});
